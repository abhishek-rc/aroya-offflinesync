import syncMetadata from './sync-metadata';
import syncQueue from './sync-queue';
import conflictLog from './conflict-log';
import syncSession from './sync-session';

export default {
  'sync-metadata': syncMetadata,
  'sync-queue': syncQueue,
  'conflict-log': conflictLog,
  'sync-session': syncSession,
};

